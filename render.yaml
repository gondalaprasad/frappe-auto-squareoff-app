services:
  - type: web
    name: frappe-app
    env: docker
    plan: starter
    autoDeploy: true

  # ✅ Redis MUST be here (NOT under databases)
  - type: redis
    name: frappe-redis
    plan: free
    ipAllowList:
      - source: 0.0.0.0/0


databases:
  - name: frappe-db
    plan: free
    ipAllowList:
      - source: 0.0.0.0/0
